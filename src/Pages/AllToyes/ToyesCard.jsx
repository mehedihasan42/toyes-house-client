import React from 'react';

const ToyesCard = ({toye}) => {
    const { name,price,rating,quantity } = toye;
    return (
        <div>
            
        </div>
    );
};

export default ToyesCard;